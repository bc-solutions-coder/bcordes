node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
coverage
test-results
playwright-report
.vinxi
todos.json

# Dolt database files (added by bd init)
.dolt/
*.db

# Git worktrees
.worktrees/

# Scratch / reference-only (Base UI migration diff refs, not part of build)
.scratch/

# AI tooling config (local-only)
.beads
.claude
.serena
.superpowers
docs/plans
docs/superpowers
CLAUDE.md
AGENTS.md
stats.html

# Beads / Dolt files (added by bd init)
.beads-credential-key
.beads/proxieddb/
