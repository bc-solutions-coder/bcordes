# Enforced behavior coverage prerequisite

Implementation for [Reach and enforce meaningful 90% coverage](https://github.com/bc-solutions-coder/bcordes/issues/90), compared with baseline commit `d0523a0`. Status: implemented, review pending.

The full workspace run passes 1,349 tests, none failed or skipped. Coverage is **93.97% lines, 92.83% statements, 90.11% branches and 90.15% functions**, with all four thresholds enforced at 90%. The include/exclude configuration and every metric denominator remain unchanged. [area-comparison.tsv](area-comparison.tsv) verifies no affected file loses coverage; [coverage-summary.json](coverage-summary.json) preserves actual measurements. The narrow branch/function margins require checking every later cleanup batch.

The runner demonstrably rejected the earlier 89.67% branch result with exit 1. After the remaining supported outcomes were covered, the unchanged threshold passed. The existing isolated partial-coverage probe now expects the threshold rejection and still checks emitted coverage output. Its reporting and enforcement obligations remain assigned to the tooling ticket; no thresholds are disabled for that probe.

## Additions and later ownership

| Test area                      | Added observable protection                                                                                                                                                                                                  | Later owner                                                                                   |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| Persistent session integration | Store lookup/destruction, isolated request tokens through actual SDK/BFF, verified identity and name fallbacks, permission denial/grant, CSRF-authorized write, unauthenticated rejection and invalid security configuration | [Authentication/server/SDK packages](https://github.com/bc-solutions-coder/bcordes/issues/93) |
| Valkey SDK integration         | Actual stored values, replacement/deletion, conditional creation, expiry and set membership                                                                                                                                  | [Query/cache/logging/utilities](https://github.com/bc-solutions-coder/bcordes/issues/94)      |
| Request middleware             | Unsupported API rejection, event authentication, BFF delegation, origin enforcement, response/header preservation and downstream error propagation through registered handlers                                               | [Shell/auth](https://github.com/bc-solutions-coder/bcordes/issues/95)                         |
| Auth error page                | Known/fallback messages and actual home/contact navigation through the router                                                                                                                                                | [Shell/auth](https://github.com/bc-solutions-coder/bcordes/issues/95)                         |
| Root error recovery            | Actual router recovery after a transient render failure and home navigation after a non-Error throw                                                                                                                          | [Shell/auth](https://github.com/bc-solutions-coder/bcordes/issues/95)                         |
| Notification stream            | Leader relay, follower heartbeat liveness, competing claims, subscriber independence, anonymous state and sign-out cache cleanup                                                                                             | [Notifications](https://github.com/bc-solutions-coder/bcordes/issues/99)                      |
| Push capability and badge      | Empty-device disable, unavailable browser API and visible 99+ cap                                                                                                                                                            | [Notifications](https://github.com/bc-solutions-coder/bcordes/issues/99)                      |
| Security response headers      | Production HTTPS policy without changing body content                                                                                                                                                                        | [Authentication/server/SDK packages](https://github.com/bc-solutions-coder/bcordes/issues/93) |

These 61 added runtime cases must be reused, not recreated, by later tickets. Five new test files and one shared disposable-server helper join the inventory. Existing auth/Valkey discovery inventories were minimally updated to admit the added files; those obsolete inventories retain their accepted later-removal dispositions. No existing test was deleted. The partial-coverage probe title was updated to describe its enforced-failure outcome.

Integration tests run actual owned session/SDK/Valkey code; only the framework request context and external HTTP responses are controlled. Each file owns a disposable loopback Valkey container with cleanup even if port discovery fails. Docker is now required for these unit integration cases and is documented in the testing guide. No live identity provider or production credential is used. Request middleware tests invoke registered handlers through a response-validating harness, not the built HTTP transport; existing and future browser checks remain separately required.

## Verification

The following deliberate defects caused focused test failure, followed by restored passing coverage execution: conditional writes overwriting existing data; dropping request cookies; accepting cross-origin writes; wrong auth error text; a disabled root reset; lost event relay; and deregistering an empty device list. [summary.json](summary.json) records these outcomes and the full run totals. These probes changed one owned implementation temporarily and restored its exact contents in `finally`; no production defect remains in the diff.

Commands: `pnpm test --coverage --reporter=json`, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `python3 scripts/check-docs.py`, and `git diff --check` pass. Relevant focused files were run throughout. No product behavior was changed; this step adds coverage and enforcement. Production build/browser baseline evidence remains in the prior ticket; this does not claim final parent implementation or remote CI completion.
