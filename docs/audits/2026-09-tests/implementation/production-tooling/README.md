# Production and Docker verification — #101

Implements [#101](https://github.com/bc-solutions-coder/bcordes/issues/101) at implementation revision `a044b93` from base `11d6ccf`, following [standalone tooling review](../../reviews/standalone-tooling/README.md). The [disposition map](dispositions.tsv) accounts for all 48 original check, command and exclusion rows. These are not 48 additional tests. [Resources](resources.tsv) records the new standalone entrypoint and CI wiring; [summary](summary.json) records commands, results, deliberate defects and image identities.

The production CLI requires each served route's expected heading and direct, correctly typed, nonempty sampled CSS and JavaScript. Its 27 Node-runner cases execute real subprocesses and controlled HTTP endpoints, including wrong pages, redirects, missing references, bad assets, lifecycle failures and cleanup. Docker command fixtures control only the external Docker boundary; separate real-container runs prove success and failure cleanup.

Docker readiness accepts direct 200 responses with a two-second request bound inside a default 30-second overall deadline. Both callback/logout URL configurations use the inspected image ID. The actual image was built for `linux/amd64` and run under emulation on a `linux/arm64/v8` Docker host. Both configurations passed. A deliberately broken image failed its route check. Neither run left verifier-owned containers or networks. These loopback checks prove public serving under two URL configurations, not live authentication, callback/logout flows, host-based routing or publication. Only the first referenced CSS and JavaScript assets are sampled.

Nine deliberate verifier defects failed their selected checks and were restored: lost route identity, followed asset redirects, empty assets, lost owned-server cleanup, accepted health redirects, lost per-request bound, lost all readiness bounds, unpinned image selection and lost Docker cleanup. The restored CLI suite passes all 27 cases.

The preceding [CI run](https://github.com/bc-solutions-coder/bcordes/actions/runs/34178671539) passed build, typecheck and lint but failed two query-inspector tests on their short timing bounds. A Linux container limited to 0.5 CPU reproduced both failures. Scoped 10-second asynchronous waits and 15-second test bounds passed in that same environment, preserving all real inspector assertions. A wrong plugin label still failed both tests; production code was restored. This is follow-up verification work from #94. The prior successful [Docker workflow](https://github.com/bc-solutions-coder/bcordes/actions/runs/34147876945) is historical handoff evidence, not a run of this change.

Final local coverage passes 1,059 Vitest cases with no failures or skips: 97.39% lines, 96.18% statements, 92.35% branches and 96.08% functions. Every measured file matches the preceding coverage result; thresholds, denominators and exclusions are unchanged. The 27 CLI cases are counted separately. Typecheck, lint, formatting, documentation links, shell syntax and production smoke pass. The real Docker build also compiles the application. Storybook runtime verification remains assigned to #102; the remaining legacy source guards remain assigned to #103.

Spec review found no gaps. Standards review found no documented violation and one maintainability suggestion: failure labels selected fixture behavior. Structured target, mode and extension fields now select behavior independently of titles.
