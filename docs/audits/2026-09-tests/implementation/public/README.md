# Public pages and contact behavior

Implementation for [issue 96](https://github.com/bc-solutions-coder/bcordes/issues/96), based on `71b68a4`. Changes are in `1ef1a43` and `fd38131`. [dispositions.tsv](dispositions.tsv) maps all 83 original cases across 13 files. Nine duplicate or structural cases were removed after their replacement outcomes passed. Project catalog, detail and filter work remains with issue 97.

Public content tests render real components, motion hooks and routing. Heading, status and region queries check the promised semantics. Hero statistics associate each value with its label; timeline periods stay associated with their employer. Featured cards associate each project's client, year and tags with its named destination. Tests preserve current published copy without claiming it is factually current or that DOM assertions establish asset availability.

Home, about, contact and resume load registered routes under an actual memory router and query provider. Page inclusion checks now require meaningful owned content. The home loader uses the real catalog with one nonfeatured metadata fixture, so substituting the full catalog fails. Visitor, customer and inquiry-staff cases establish permission-dependent invitations. The resume link retains its PDF destination and download semantics; this DOM check does not establish PDF retrieval or contents.

The real contact form validates empty fields without submitting, sends exact entered values, normalizes omitted optional values, disables pending submission, and renders success. Send Another Message restores empty editable fields and selector placeholders. A rejected operation renders the real toaster feedback, preserves values and allows a successful retry. Known identity fields are populated and locked; missing identity fields remain editable. The controlled server-call boundary establishes client behavior. The existing isolated guest-browser cases from issue 92 establish actual backend payloads, normalization, no operation on invalid input, and reset with no second operation.

## Review and verification

The spec review found that both published projects being featured concealed a wrong home-loader selector. The replacement fixture and an independent full-catalog mutation now prove this distinction. The follow-up spec review has zero remaining findings. Standards review found no documented violations and one nonblocking suggestion to extract the short repeated motion setup; the explicit setup remains readable and avoids another lifecycle abstraction.

Nineteen deliberate defects failed with relevant rendered or request assertions, then source was restored in `finally`: wrong inquiry name, missing reset, missing failure feedback, unlocked name, wrong optional normalization, removed validation, missing heading/status, swapped statistics, ignored permission, wrong destination, wrong card client, unlimited tags, wrong career period, empty home loader, missing about introduction/contact form, missing PDF download attribute, and use of the full catalog on home. [summary.json](summary.json) records results and [coverage-summary.json](coverage-summary.json) preserves per-file measurements.

Three stale migration inventory entries blocked removal of owned-component mocks: contact route test as a contact importer, home route test as a projects importer, and resume route test as a UI importer. Those entries were removed without adding source assertions. The two expanded app cases remain accounted for by issue 103; the pruned UI importer is inside a retained issue 100 case. Remaining files stay with those owners.

All 1,212 workspace tests and 33 browser tests pass without failures or skips. Coverage is 95.07% lines, 93.93% statements, 90.61% branches and 92.40% functions. Denominators and thresholds are unchanged; no retained file loses covered lines, statements, branches or functions. Typecheck, lint, formatting, documentation links and diff checks pass. Final commands and measured results are recorded in summary.json. Browser verification uses the unchanged production artifact from issue 95; this slice changes test code only. Local checks do not claim remote CI success.
