# Test and verification entry points

Inventory at `990edf5a5ee6142dc8331d340c313e76f28e9a2d`. These checks are accounted for separately from the 124 named test files. Their future disposition remains pending.

| Entry point                                                      | Discovery or actual checks                                                                                                                                                                            | Current integration and prerequisites                                                                                                                                                 |
| ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root `pnpm test` / `pnpm exec vitest run --coverage`             | Root projects `apps/*`, `packages/*`; 14 project configs; 120 test files. Web uses `src/**/*.test.ts` and `src/**/*.test.tsx`. Packages select root `*.test.ts` and/or `src` tests.                   | CI test job runs coverage; V8 summary includes application/package source with documented exclusions. Installed workspace dependencies required.                                      |
| `pnpm --filter bcordes exec playwright test`                     | `apps/web/playwright.config.ts`: `e2e/**/*.spec.ts`, Chromium; 24 cases in four files. Global setup starts fixture backend and disposable Valkey; webServer starts the production Node artifact.      | CI build job and documented local command. Requires build, Chromium, Docker or dedicated `E2E_VALKEY_URL`; CI retries twice, local run has zero retries. `forbidOnly` active in CI.   |
| `pnpm build`                                                     | Vite production build, including server output and client assets.                                                                                                                                     | CI build job; existing dependencies and build configuration. Passed in baseline.                                                                                                      |
| `scripts/verify-production.sh` / `scripts/verify-production.mjs` | Real HTTP status/content type and HTML for `/`, `/about`, `/projects`, `/contact`; CSS/JS asset existence, type, nonempty response. Node assertions inspect built responses, not implementation text. | Wrapper starts built server with synthetic settings, or accepts an existing URL. CI build job; passed locally. No live external-service assurance.                                    |
| `scripts/verify-docker.sh`                                       | Builds Docker image with package token secret; runs disposable Valkey and app containers; checks two runtime redirect configurations through production smoke script.                                 | Docker publish workflow smoke job. Requires Docker and `NODE_AUTH_TOKEN` with package read access. Not run in this inventory task; native build/browser baseline was run.             |
| `pnpm typecheck`                                                 | Recursive workspace `tsc --noEmit`, including the config package.                                                                                                                                     | Dedicated CI job; not rerun for this documentation/extractor change. This is a compiler check, not a behavioral test count.                                                           |
| `pnpm lint`                                                      | Shared ESLint rules and module boundaries.                                                                                                                                                            | Dedicated CI job. Targeted ESLint run on the new inventory script passed.                                                                                                             |
| `pnpm format`, `pnpm check`, `.husky/pre-commit`                 | Formatting; `check` also applies lint fixes; pre-commit invokes `pnpm lint-staged`.                                                                                                                   | Mutating developer checks, not test runs. Hook processes staged files; it is not a substitute for suite execution.                                                                    |
| `pnpm --filter bcordes build-storybook`                          | Storybook bundle compilation.                                                                                                                                                                         | Manual app script; not an additional test runner or CI test job. Not run.                                                                                                             |
| `scripts/check-docs.py`                                          | Tracked/untracked repo Markdown links and heading anchors, excluding `.agents`.                                                                                                                       | Manual documentation quality gate. Source/document inspection rather than product behavior. Run on this audit's documentation.                                                        |
| `scripts/check-comment-edits.mjs <git-ref> [scope]`              | Parses/transpiles before/after source and compares emitted syntax; rejects additions/deletions in scope and requires some changed JS/TS.                                                              | Historical comment-only verification utility, not CI. No applicable comment-only edit in this task; not run. Needs a later explicit tooling disposition under the source-test policy. |
| `docs/audits/2026-09-documentation/verify-coverage.py`           | Reconciles a historical Git snapshot against audit rows, blobs, statuses, and rationale fields.                                                                                                       | Audit bookkeeping; its coverage means file-review coverage, not V8 test coverage. Not run for this separate audit.                                                                    |
| `docs/audits/2026-09-documentation/verify-completion.py`         | Compares historical audit snapshot, execution records, excluded-file preservation, and current paths.                                                                                                 | Historical audit bookkeeping with source/file structure assertions. Not run for this separate audit; pending tooling review.                                                          |
| `.agents/skills/**` shell templates and guardrail scripts        | Reusable agent setup/guardrail resources, not repository test entry points.                                                                                                                           | Not invoked by product test commands or CI. Excluded from runtime test inventory, with this explicit accounting.                                                                      |

Package-level tests can invoke nested Vitest processes, inspect configuration, build CSS, or run workflow scripts. Those assertions are recorded under their parent test, not counted again as independent test files. See the candidate inspection for examples. Test fixture/helper files under `e2e/fixtures`, package `src/testing`, and `packages/test-utils` support these entry points; they are not standalone discovered tests.

The release/deploy workflows handle publishing and deployment rather than introducing additional test runners. No additional tracked JavaScript/Python/shell test runner was found outside the entries above. This inventory script is a new planning asset; it inventories source declarations and reconciles runner reports but does not enforce source structure in the product suite.
